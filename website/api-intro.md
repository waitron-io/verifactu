# API reference

The `index` module contains the public record, hash, QR, XML, validation, and AEAT client APIs.
The `testing/fake-aeat` module contains the offline transport for application tests.

Start with the [submission guide](/verifactu/en/guides/submit/) if you want to see these functions
in an end-to-end workflow.
